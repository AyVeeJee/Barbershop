<div id="form-holder" class="form-calendar">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button id="form-btn" class="btn btn-md btn-primary form-btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
</div>