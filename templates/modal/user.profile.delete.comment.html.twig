<div class="modal fade" id="deleteCommentModal" tabindex="-1" role="dialog" aria-labelledby="deleteCommentModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="btn-close" id="close" data-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form id="comment-modal" style="text-align: center" method="POST"
                      action="{{ path('user_profile_comment_delete') }}">
                    <h4 class="modal-title" id="deleteCommentModalLabel">Are you sure you want to delete your
                        comment?</h4>
                    <div class="modal-footer">
                        <button type="submit" id="delete-comment-btn-save" name="employee_id" value=""
                                class="btn btn-primary">Save
                        </button>
                        <button type="button" id="delete-comment-btn-close" class="btn btn-secondary"
                                data-dismiss="modal">Close
                        </button>
                    </div>
                </form>
            </div>

        </div>
    </div>
</div>